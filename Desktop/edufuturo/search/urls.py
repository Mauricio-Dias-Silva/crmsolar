# search/urls.py
from django.urls import path

app_name = 'search' # Importante para namespace

urlpatterns = [
    # Suas URLs do app 'search' virão aqui
    # Exemplo: path('', views.search_results, name='results'),
]