# notifications/urls.py
from django.urls import path

app_name = 'notifications' # Importante para namespace

urlpatterns = [
    # Suas URLs do app 'notifications' virão aqui
    # Exemplo: path('', views.notification_list, name='list'),
]